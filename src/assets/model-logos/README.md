# Model Logos

Place model logo images here as PNG files.

The filename should match the model identifier used in the config (e.g., `llama.png`, `gpt.png`, `mistral.png`).

Recommended image specifications:

- Format: PNG with transparency
- Size: 32x32 pixels or 64x64 pixels
- Square aspect ratio
